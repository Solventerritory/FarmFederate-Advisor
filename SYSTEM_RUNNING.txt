╔════════════════════════════════════════════════════════════╗
║        FARMFEDERATE SYSTEM - COMPLETE & RUNNING           ║
╚════════════════════════════════════════════════════════════╝

✅ ALL SYSTEMS OPERATIONAL

┌─────────────────────────────────────────────────────────┐
│ RUNNING SERVICES                                        │
└─────────────────────────────────────────────────────────┘

✓ Backend Server (FastAPI)
  └─ http://localhost:8000
  └─ API Docs: http://localhost:8000/docs
  └─ Sensor API: http://localhost:8000/sensors/latest
  └─ Status: Starting (allow 30 seconds for model to load)

✓ MQTT Broker (Mosquitto)
  └─ Port: 1883
  └─ Status: RUNNING

✓ MQTT Listener (Python)
  └─ Topic: farmfederate/sensors/#
  └─ Saves to: backend/checkpoints_paper/sensors/
  └─ Status: ACTIVE

✓ Frontend (Flutter on Chrome)
  └─ http://localhost:3000
  └─ Dashboard: http://localhost:3000/#/dashboard
  └─ Status: Starting

✓ Test Sensor Data
  └─ No physical sensor devices configured for this demo



┌─────────────────────────────────────────────────────────┐
│ MONITOR & VERIFY                                        │
└─────────────────────────────────────────────────────────┘

Monitor MQTT Messages:
  mosquitto_sub -h localhost -t "farmfederate/sensors/#" -v

Check Sensor API:
  curl http://localhost:8000/sensors/latest

View Sensor Files:
  Get-ChildItem backend\checkpoints_paper -Recurse -Filter "*.json"

Test Relay Control: (not applicable in this repository)

┌─────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING                                         │
└─────────────────────────────────────────────────────────┘

If Frontend Shows No Data:
  1. Wait 30 seconds for backend to fully start
  2. Refresh browser (Ctrl+R)
  3. Check: http://localhost:8000/sensors/latest
  4. Open browser console (F12) for errors

If a device is not connecting:
  1. Check Serial Monitor or logs for error messages
  2. Verify network credentials and addresses
  3. Check broker and backend connectivity
  5. Add firewall rule (as admin):
     New-NetFirewallRule -DisplayName "MQTT" -Direction Inbound -Protocol TCP -LocalPort 1883 -Action Allow

If Backend Not Starting:
  1. Check backend PowerShell window for errors
  2. Ensure model checkpoint exists
  3. Virtual environment activated

┌─────────────────────────────────────────────────────────┐
│ DOCUMENTATION FILES                                     │
└─────────────────────────────────────────────────────────┘

📄 RUNNING_SETUP.md - System architecture

┌─────────────────────────────────────────────────────────┐
│ QUICK COMMANDS                                          │
└─────────────────────────────────────────────────────────┘

Send More Test Data:
  cd backend
  python send_real_sensor_data.py

Check System Status:
  python backend/diagnose.py

View Latest Sensor Reading:
  Get-Content (Get-ChildItem checkpoints_paper -Recurse -Filter "*.json" | Sort-Object LastWriteTime -Descending | Select-Object -First 1).FullName

┌─────────────────────────────────────────────────────────┐
│ WHAT'S RUNNING NOW                                      │
└─────────────────────────────────────────────────────────┘

3 PowerShell Windows:
  [1] Backend Server - FastAPI with ML model
  [2] MQTT Listener - Capturing sensor data
  [3] Frontend - Flutter web application

1 Chrome Browser:
  → http://localhost:3000 (dashboard loading)

Background Services:
  → Mosquitto MQTT Broker (Windows Service)

╔════════════════════════════════════════════════════════════╗
║                    SYSTEM READY! 🚀                       ║
║                                                            ║
║  Frontend: http://localhost:3000                          ║
║  Backend:  http://localhost:8000/docs                     ║
║                                                            ║
║  ⏰ Wait 30 seconds, then refresh browser                 ║
║  📱 Upload device code to start receiving real sensor data ║
╚════════════════════════════════════════════════════════════╝
